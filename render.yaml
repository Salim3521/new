services:
  - type: web
    name: factureia-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: factureia-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
    envVars:
      - key: PORT
        value: 4000
